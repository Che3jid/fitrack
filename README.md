# FitTrack

一个手机优先的开源健身饮食记录项目。网页端使用 HTML、CSS 和 TypeScript；iPhone 端使用 SwiftUI、WKWebView 和 HealthKit，并复用同一套界面与本地数据逻辑。

GitHub 仓库：[Che3jid/fitrack](https://github.com/Che3jid/fitrack)。产品名称为 FitTrack，仓库名为 `fitrack`。

## 当前进度

**本地优先版本，已加入可选后端数据库。** 已实现首次设置、个人资料、饮食/训练/体重记录、今日实时汇总、7/30/90 日趋势、动态 TDEE 试算与每日摄入建议，以及 JSON 备份与恢复。日常记录先保存在设备的 IndexedDB；用户可连接自己的 SQLite 服务，核对后手动上传或下载。

饮食按四餐管理，可手动填写，也可从本地食材库按原料重量、调味料、留在菜中的油和成品重量计算后记入饮食。训练手动记录时长和消耗，体重每日一条；均可按日期查看、编辑、删除和恢复。趋势保留缺失日期，不把缺失当成零；摄入的 7 日平均只统计用户明确标记完成的日期。备份导入会先展示摘要，确认后完整替换本地数据，并保留最近一次导入前的恢复副本。

## V1 功能规划

- 首次设置：性别、年龄、身高、体重、活动水平和健身目标。
- 今日：BMR、TDEE、目标热量、摄入、预计消耗、热量差与三大营养素。
- 记录足够时：根据近 28 天完整饮食和体重趋势，显示保守的动态 TDEE 试算及目标摄入参考值。
- 饮食：早餐、午餐、晚餐、加餐的食物增删改；本地基础食材库与配方计算。
- 训练：类型、时长及手动填写的估算消耗。
- 趋势：体重、摄入、热量差与 7 日平均值。
- 我的：修改资料和目标；本地数据导出、导入；手动连接后端数据库。

不包含 AI 食物识别或社交；食物库先覆盖常用基础项，不声称收录所有品牌或菜品。

## 技术栈

- HTML + CSS + 严格模式 TypeScript，无 UI 框架。
- [Vite](https://vite.dev/guide/)：开发服务器和生产构建。
- [Vitest](https://vitest.dev/guide/)：纯计算模块单元测试。
- [GSAP](https://gsap.com/docs/v3/GSAP/)：页面进入、热量仪表和按键反馈；遵循系统“减少动态效果”设置。
- Git / GitHub：版本控制；CI 在后续阶段配置。
- IndexedDB：按资料、食物、训练、体重、每日状态和目标快照分集合保存，以事务保证一次更新完整写入；业务服务只依赖异步存储接口。
- Node.js 内置 SQLite：后端按相同集合持久化记录；访问密钥、原子替换和修订版本检查保护手动同步。
- [USDA FoodData Central SR Legacy](https://fdc.nal.usda.gov/download-datasets/)：本地食材和调味料的每 100 g 营养参考值；每项保留 FDC ID，数据按 CC0 提供。
- SwiftUI + WKWebView：提供原生 iPhone App 外壳，并在构建时把网页产物打包进 App。
- HealthKit：已加入权限桥接，为后续读取 Apple Watch 同步到 iPhone 的体重、步数、心率、运动和活动能量做准备。

## 运行

需要 Node.js 24 LTS（随附 npm）。在项目根目录执行：

```sh
npm ci
npm run dev
```

打开终端输出的本地地址。不能直接双击 `index.html` 运行 TypeScript 源码。

```sh
npm run test        # 单次测试
npm run test:watch  # 开发时持续测试
npm run typecheck  # 类型检查
npm run build      # 类型检查并构建到 dist/
npm run preview    # 预览已经构建的产物
npm run server     # 启动本机 SQLite 数据库服务
npm run check      # 前后端测试、类型检查和构建
```

Mac 与 iPhone 连接同一可信局域网后，可以执行 `npm run dev -- --host 0.0.0.0`，在 Safari 访问终端展示的局域网地址。必要时允许 macOS 防火墙的本地网络访问。此方式仅用于开发预览；后续 PWA 的离线能力需在 HTTPS 环境验证。

`dist/` 是后续静态部署的产物。GitHub 仓库用于托管源码，项目网页目前没有部署到公网。

### iPhone 版

需要 Xcode 以及已经安装好的 Node.js。在 Xcode 中打开 `ios/FitTrack.xcodeproj`，选择 `FitTrack` Target，然后在 **Signing & Capabilities** 中选择自己的 Apple Developer Team。选择一台 iPhone 模拟器后点击运行；Xcode 会自动构建网页并打包到 App 中。

模拟器可以验证界面和本地记录。HealthKit 的真实健康数据需要使用实体 iPhone，并在系统授权弹窗中允许 FitTrack 读取。当前版本只完成权限连接，尚未把健康数据自动写入 FitTrack 记录。

### 可选后端数据库

另开一个终端运行 `npm run server`。服务默认只监听 Mac 的 `127.0.0.1:8787`，首次启动会在忽略提交的 `data/` 目录创建 `fittrack.sqlite` 和 `access-token`，并在终端显示访问密钥。在网页“我的 → 连接数据库”填写 `http://127.0.0.1:8787` 与密钥，先查看两端记录数量，再明确选择上传或下载。上传完整替换服务器记录；下载完整替换当前设备记录，并保存一次可从“数据备份”恢复的旧副本。服务器修订版变化时会拒绝旧预览的上传，不自动合并。

实体 iPhone 访问 Mac 上的数据库时，两台设备需处于同一可信局域网，在 Mac 上运行 `FITTRACK_HOST=0.0.0.0 npm run server`，然后在 App 中填写 `http://<Mac 局域网 IP>:8787`。iOS 会在首次访问时请求本地网络权限。局域网 HTTP 不提供传输加密，不要在公共 Wi-Fi 使用；对公网部署前必须配置 HTTPS 和正式账号认证。不要把访问密钥、数据库文件或备份提交到 GitHub。

## 当前目录

```text
src/
  domain/
    models.ts          数据模型，无浏览器依赖
    calculations.ts    公式、输入校验、营养汇总、每日收支、7 日平均
    adaptive-tdee.ts    动态 TDEE 与摄入建议的纯计算
    records.ts         饮食、训练、体重输入规则
  components/          公共布局、展示标签与原生 SVG 图表
  pages/               首次设置、今日、记录、趋势、资料与备份页面
  services/            资料、记录、汇总、趋势、建议与备份业务逻辑
  storage/             异步接口、IndexedDB、旧数据迁移及结构校验
  utils/               上海日期键、随机 ID、HTML 转义
  styles/              通用样式和 iPhone 深色界面样式
  main.ts              路由和应用入口
tests/                 计算、存储、记录、趋势、备份、迁移和异常恢复测试
docs/architecture.md   分层、路由、持久化和阶段规划
ios/                   SwiftUI iPhone 工程、HealthKit 桥接与 Xcode 配置
server/                SQLite 数据库、HTTP API 与后端测试
```

## 使用方式与本地数据

1. 首次访问自动进入 `#/onboarding`，填写完整资料，查看计算预览并保存。
2. `#/today` 展示当前业务日期的目标、BMR 和 TDEE。
   记录达到观察条件后，同页会显示动态 TDEE 试算与每日摄入建议；不足时显示当前记录进度。
3. `#/profile` 查看资料，`#/profile/edit` 修改或取消修改。
4. `#/diet` 按四餐记录食物，可选择日期补录；营养字段填写本次实际食用总量，修改重量不会自动换算。
   `#/library` 可按食材和调味料重量创建配方、计算整道菜及本次食用份，并直接记入饮食。
5. `#/training` 记录运动类型、时长和手动估算消耗。
6. `#/weight` 每天保存一条体重，可从今日或我的进入。记录页选择日期，再新增或修改当天体重。
7. `#/trends` 查看以指定日期结束的 7、30 或 90 日体重、摄入、热量差及 7 日平均；图表下方保留精确的每日明细表。
8. `#/backup` 下载完整 JSON 备份，或选择备份文件查看摘要后导入。导入是完整替换，不与当前记录合并。
9. `#/sync` 连接后端数据库，预览本机与服务器记录，再手动上传或下载。
10. 删除后在同一日期的“最近删除”中恢复，不提供永久清除入口。

数据保存在 IndexedDB 数据库 `fittrack-local` 中，资料、计划快照、体重、饮食、训练和饮食完成状态使用独立对象存储。所有集合在同一个事务中更新，写入失败时保留原状态。首次打开新版时会校验并迁移旧 localStorage 键 `fittrack.state.v1`；只有数据库提交成功后才移除旧副本。损坏数据或不支持的版本不会被自动清空。

食物新增、编辑、删除、恢复会将受影响日期重新标为“记录中”；标记当日记录完成代表当天饮食已完整，包括用户明确确认的零摄入日。未完成日期的热量差是基于当前已记录摄入的临时值。

### 食物库与烹饪计算

首批 48 项本地基础食物包含谷物、肉蛋奶、豆类、蔬菜水果、盐糖酱料和食用油，均可离线查询。数值来自 USDA FoodData Central [SR Legacy 2018 下载](https://fdc.nal.usda.gov/download-datasets/)，按每 100 g 可食部分计；点击库中每项的 USDA 链接可查看对应 FDC ID。它们是参考值，不能代表所有中国品牌、品种和烹调后的实测值。

整道菜的热量、蛋白质、碳水、脂肪和钠，按每种食材或调味料的 **每 100 g 数值 × 实际用量** 求和；食用油只计入实际留在食物里的克数。填写成品总重后，换算每 100 g 和本次食用重量对应的营养。蒸、煮、煎、炒、烤、炸本身不附加固定热量；水分变化只影响单位重量浓度，丢弃的汤汁、油脂或裹粉需要按实际吃下的量调整。此版不自动套用不同食材的营养保留率或吸油率，详见 [USDA 的烹饪产率与保留率说明](https://www.ars.usda.gov/ARSUserFiles/80400525/Articles/jfca10_102-114.pdf)。

钠可随食物记录保存；当日只合计已知钠的记录，缺少钠数据的旧记录不当作 0。显示的“折合食盐”按钠的克数 × 2.5 估算，依据 [WHO 的换算说明](https://www.who.int/news-room/fact-sheets/detail/sodium-reduction)。

体重按日期唯一。最新有效体重同步到资料并更新今日计划，补录早于最新记录的日期不会覆盖当前体重。删除最新体重会回退到最近有效记录，恢复后再次更新；全部删除时保留资料中的最后已知体重。所有操作均保留过去日期的计划快照。

业务日期固定使用 `Asia/Shanghai`。打开新一天时生成当日计划，不补造未访问日期或体重；编辑资料只更新当天目标。今日页跨午夜会自动刷新，编辑中的表单不会被定时刷新覆盖。

IndexedDB 事务会串行处理多标签页写入，BroadcastChannel 会通知其他标签页刷新未编辑的页面。本地数据库仍按网站来源隔离，`localhost`、`127.0.0.1`、不同端口和未来线上域名不会自动共用数据；跨来源迁移请使用 JSON 备份。

未加入账号、自动云同步或遥测。JSON 备份文件只在当前设备生成和读取，不会因导出操作上传；数据库同步则仅在用户主动选择后传输资料和记录。导入或数据库下载只保留最近一次替换前的恢复副本；长期使用仍应定期下载外部备份。

## 计算口径

使用 Mifflin–St Jeor 公式估算静息能量消耗，界面沿用“BMR（估算）”。[原始研究](https://pubmed.ncbi.nlm.nih.gov/2305711/)

```text
男性：10 × 体重kg + 6.25 × 身高cm − 5 × 年龄 + 5
女性：10 × 体重kg + 6.25 × 身高cm − 5 × 年龄 − 161
TDEE = BMR × 活动系数
```

| 活动水平 | 系数 |
| --- | --- |
| 久坐 | 1.2 |
| 轻度 | 1.375 |
| 中度 | 1.55 |
| 高度 | 1.725 |
| 极高 | 1.9 |

减脂、维持、增肌的目标分别为 TDEE × 0.85、1、1.10。这些是产品默认策略，不是针对个人的医疗或营养处方。V1 暂限成人；校验范围为年龄 18–100 岁（整数）、身高 100–250 cm、体重 30–350 kg，边界为产品输入约束。

- TDEE 已包含活动水平中的平均训练消耗，训练记录不额外叠加到预计总消耗。
- 热量差 = 摄入 − TDEE；负数表示缺口。
- 距目标剩余 = 目标摄入 − 摄入；负数表示超出目标。
- 食物重量和营养字段均代表实际食用份量；不会用 4/4/9 强行覆盖输入热量。
- 保留内部小数精度，只在 UI 展示时格式化；热量取整数，营养素汇总最多保留一位小数。
- 7 日窗口包含指定日期及前 6 天，按日历计算，不是最近 7 条记录。
- 缺失值用 `null` 表示，不当作零；没有有效数据时平均值为 `null`。
- 摄入平均值由业务层筛选已完成饮食记录的日期后计算，同时展示有效天数。每日数值重复则拒绝计算，避免重复加权。
- 趋势可查看 7、30 或 90 个日历日；为计算窗口开头的 7 日平均，会额外读取此前 6 天，但不会把它们画入所选区间。
- 未完成饮食日期的摄入和热量差以空心点显示，表示临时值；没有当日计划时热量差保持缺失。
- 业务层以 `Asia/Shanghai` 产生日期键，并保存每日目标快照，资料变更不覆盖历史。

动态 TDEE 只使用最近 28 个日历日中已标记完成的饮食记录，以及未删除的体重记录。需要至少 21 天完整饮食、6 次称重、首周和末周各至少 2 次称重，且称重跨度至少 21 天。由体重趋势和平均摄入得到的观察值会向当前 TDEE 基线收缩，并限制调整幅度不超过基线的 10%；变化过快或记录明显不一致时不提供建议。换算中使用的约 7,700 kcal/kg 是粗略经验量，水分变化等会带来误差。建议仅作自我记录的参考，不自动修改每日目标或历史计划，也不叠加训练记录中的消耗。

## Roadmap

- [x] 第一阶段：工程、计算模块、测试、基础预览、MIT License。
- [x] 第二阶段：首次设置、今日、我的、本地持久化和每日目标快照。
- [x] 第三阶段：饮食、训练、体重记录与可恢复删除。
- [x] 第四阶段：趋势、数据导入导出、手机体验。
- [x] 本地 IndexedDB：分集合存储、原子事务、旧数据自动迁移和跨标签页刷新。
- [x] 可选 SQLite 后端：访问密钥、手动双向同步、冲突拒绝和下载前本机恢复副本。
- [x] 基础食物库：可追溯的原料营养值、调味料、食用油及配方分份计算。
- [x] GitHub 开源仓库。
- [x] iPhone 基础工程：SwiftUI、内置网页、HealthKit 权限桥接。
- [x] 近 28 天记录驱动的保守动态 TDEE 试算与每日摄入建议。
- [ ] 微信登录：待准备微信开放平台移动应用配置与服务端身份交换；当前继续使用本地数据和备份。
- [ ] 第五阶段：CI、静态部署、PWA。
- [ ] 后续：从 HealthKit 导入 Apple Watch 数据、正式账号与自动云同步；不在 V1 引入 AI 食物识别。

未主动上传的记录仍只保存在当前设备，清除网站数据可能丢失记录；请定期从“我的 → 数据备份”下载 JSON 文件。

## 贡献与许可

提交变更前运行 `npm run check`。计算规则变更需同步测试、文档和计算版本；业务公式不要放进界面事件处理函数。不要提交个人饮食记录、导出备份或凭证。

本项目自身代码使用 [MIT License](LICENSE)。GSAP 是独立依赖，遵循其 [Standard “No Charge” License](https://gsap.com/community/standard-license/)。
