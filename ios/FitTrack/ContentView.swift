import SwiftUI

struct ContentView: View {
    var body: some View {
        FitTrackWebView()
            .background(Color(red: 0.051, green: 0.055, blue: 0.055))
            .preferredColorScheme(.dark)
    }
}
