import SwiftUI

struct ContentView: View {
    var body: some View {
        FitTrackWebView()
            .background(Color(red: 0.82, green: 0.82, blue: 0.80))
            .preferredColorScheme(.light)
    }
}
