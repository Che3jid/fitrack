import SwiftUI

struct ContentView: View {
    var body: some View {
        FitTrackWebView()
            .background(Color(red: 0.008, green: 0.024, blue: 0.09))
            .preferredColorScheme(.dark)
    }
}
